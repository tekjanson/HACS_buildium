{
  "config": {
    "step": {
      "user": {
        "title": "buildium_iot",
        "description": "Once you have created an entry, refer to the [docs](https://hacs-buildium_iot.readthedocs.io/en/latest/) to learn how to create scripts and functions.",
        "data": {
          "allow_all_imports": "Allow All Imports?",
          "hass_is_global": "Access hass as a global variable?",
          "buildium_client_id": "add the id for this instance",
          "buildium_secret": "add the secret for this instance"
        }
      }
    },
    "abort": {
      "already_configured": "Already configured.",
      "single_instance_allowed": "Already configured. Only a single configuration possible.",
      "updated_entry": "This entry has already been setup but the configuration has been updated."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Update buildium_iot configuration",
        "data": {
          "allow_all_imports": "Allow All Imports?",
          "hass_is_global": "Access hass as a global variable?",
          "buildium_client_id": "add the id for this instance",
          "buildium_secret": "add the secret for this instance"
        }
      },
      "no_ui_configuration_allowed": {
        "title": "No UI configuration allowed",
        "description": "This entry was created via `configuration.yaml`, so all configuration parameters must be updated there. The [`buildium_iot.reload`](developer-tools/service) service will allow you to apply the changes you make to `configuration.yaml` without restarting your Home Assistant instance."
      },
      "no_update": {
        "title": "No update needed",
        "description": "There is nothing to update."
      }
    }
  }
}